# Vercel Edge Function proxy — NAP Transportes
# La API key NUNCA se incluye en el repo. Se configura como variable de entorno en Vercel.
# En desarrollo local, crea un fichero .env.local con:
# NAP_API_KEY=tu-api-key-aqui

NAP_API_KEY=
